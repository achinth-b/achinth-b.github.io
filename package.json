{
  "name": "achinth-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 8765"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "marked-footnote": "^1.4.0"
  }
}
